#include "../include/shell.h"

// в хедере объявлена переменная t_global

// int main(int argc, char *argv[], char **envp)  примерно так можешь начать мейник здесь объявление
// {												глобальной переменной и инициализация переменной среды
// 	init_env(&g.root, envp);
// 	g.argv = argv;
// 	t_list *next = g.root;
// 	delete_list(&g.root);
// }